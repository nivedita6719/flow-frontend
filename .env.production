# Production API base URL.
# Vercel's dashboard env var (Project → Settings → Environment Variables)
# overrides this at build time if set; keep them in sync.
REACT_APP_API_URL=https://flow-backend-plq5.onrender.com/api
